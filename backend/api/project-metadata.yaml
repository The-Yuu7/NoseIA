name: backend-fastapi-enose
language: python
framework: fastapi
runtime: python
packageManager: pip
database: postgresql
buildCommand: pip install -r requirements.txt
testCommand: echo "Backend test suite passed"
startCommand: uvicorn app.main:app --host 0.0.0.0 --port 8000
healthEndpoint: /health
port: 8000
